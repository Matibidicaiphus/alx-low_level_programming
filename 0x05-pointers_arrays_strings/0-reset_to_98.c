#include "main.h"
#include <stdio.h>

/**
 * Takes a pointer to an int as parameter and updates the value it points.
 * @n: input integer.
 * Return: Always 0.
 */
void reset_to_98(int *n)
{
	*n = 98;
}
